#include "defs.h"
#include "Perceptron.h"

int main(int argc, char* argv[]){
  Perceptron* p = new Perceptron;
  vector<int>* foo;
  
  foo->push_back(12);
  
  cout << (*foo)[0];
  //p->process(foo);
}
